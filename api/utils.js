// API Utility Functions
// This file can be used to store shared helper functions or constants
// that are used across multiple API endpoints.

/**
 * Example utility function:
 *
 * export const formatErrorResponse = (message, statusCode = 500) => {
 *   return {
 *     success: false,
 *     error: {
 *       statusCode,
 *       message,
 *     },
 *   };
 * };
 */

// Add your shared utility functions and constants here.
// For example:
// export const COMMON_API_ERROR_MESSAGE = "An unexpected error occurred.";

console.log("api/utils.js loaded. Add shared API utilities here.");
